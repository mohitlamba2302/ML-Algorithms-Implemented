Train data should be mixture of both the spams and not spam mails collected in a directory and saves as .txt

Test data can be any number of mails saved as a text file and collected in a directory and saved as .txt.

bth these directories should be subdirectories to the main code directory.